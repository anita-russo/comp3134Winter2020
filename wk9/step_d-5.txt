The browser displays one directory up from the current directory, html, which is
the www directory, accessed using ../ (or %2e, %2e, %2f)

By accessing this directory you are accessing the default root folder of the
web server.
